/*
    You are given a list of two nodes that reflect the structure
*/
interface TreeNode {
  parent: TreeNode | null;
}

/*
    Implement a function that finds the common ancestor of both nodes or returns false
    if they do not share an ancestor.
*/
export const getCommonAncestor = (
  nodeOne: TreeNode,
  nodeTwo: TreeNode
): TreeNode | false => {
  /* write code here */

  return false;
};
